{
  "name": "factoriollm",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace packages/compiler && npm run build --workspace packages/smoke && npm run build --workspace packages/cli"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
